node_modules/
/dist/
*.tsbuildinfo

# Generated at build:worker time by scripts/gen-handlers.mjs (Workers has no fs,
# so the handler list is baked into a static-import manifest). Regenerated by the
# deploy container on every build — never commit it.
src/handlers.generated.ts
